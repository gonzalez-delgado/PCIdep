% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util.R
\name{test_statistic}
\alias{test_statistic}
\title{V-norm test statistic for the difference of two cluster means}
\usage{
test_statistic(cl, clusters, X, U, Sigma)
}
\arguments{
\item{cl}{Integer (or factor) vector of length \eqn{n} giving the cluster
assignment of each observation.}

\item{clusters}{Integer vector of length 2 specifying the pair of clusters
to compare. Both entries must be present in \code{cl}.}

\item{X}{A numeric \eqn{n \times p} data matrix.}

\item{U}{An \eqn{n \times n} positive-definite matrix describing the
dependence structure between the rows of \code{X}.}

\item{Sigma}{A \eqn{p \times p} positive-definite matrix describing the
dependence structure between the columns of \code{X}.}
}
\value{
A numeric scalar giving the test statistic (V-norm of the
  difference of cluster means).
}
\description{
Computes the test statistic shared by the post-clustering inference
functions \code{test.clusters.hc}, \code{test.clusters.km}, and
\code{test.clusters.MC}: the V-norm of the difference between the means
of two selected clusters, \eqn{\|\nu^\top X\|_V = \sqrt{(\nu^\top X)^\top V^{-1} (\nu^\top X)}},
where \eqn{\nu^\top X} is the difference of cluster means and
\eqn{V = (\nu^\top U \nu)\,\Sigma}, with \eqn{\nu} the contrast vector
between the two selected clusters.
}
\examples{
n <- 30; p <- 5
X <- matrix(rnorm(n * p), nrow = n)
cl <- rep(1:3, each = 10)
test_statistic(cl = cl, clusters = c(1, 2), X = X, U = diag(n), Sigma = diag(p))

}
\seealso{
Other Utilities: 
\code{\link{ARI}()},
\code{\link{is.CS}()},
\code{\link{preserve.cl}()},
\code{\link{setup.model}()},
\code{\link{validate_hc_setting}()},
\code{\link{validate_km_candidate}()},
\code{\link{validate_km_setting}()},
\code{\link{validate_ndraws}()},
\code{\link{validate_seed}()}
}
\concept{Utilities}
