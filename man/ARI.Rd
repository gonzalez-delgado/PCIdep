% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util.R
\name{ARI}
\alias{ARI}
\title{Adjusted Rand Index}
\usage{
ARI(cl1, cl2)
}
\arguments{
\item{cl1}{Integer (or factor) vector of length \eqn{n} giving the first
cluster assignment.}

\item{cl2}{Integer (or factor) vector of length \eqn{n} giving the second
cluster assignment.}
}
\value{
A numeric scalar in \eqn{(-1, 1]} giving the Adjusted Rand Index.
}
\description{
Computes the Adjusted Rand Index (ARI) between two cluster assignment
vectors. The ARI measures the similarity between two partitions, corrected
for chance. It equals 1 for identical partitions and has an expected value
of 0 for independent (random) partitions.
}
\examples{
# Perfect agreement
cl1 <- c(1, 1, 2, 2, 3, 3)
cl2 <- c(2, 2, 1, 1, 3, 3)  # same partition, different labels
ARI(cl1, cl2)  # should be 1

# Random agreement
set.seed(1)
cl1 <- sample(1:3, 100, replace = TRUE)
cl2 <- sample(1:3, 100, replace = TRUE)
ARI(cl1, cl2)  # close to 0

}
\seealso{
Other Utilities: 
\code{\link{is.CS}()},
\code{\link{preserve.cl}()},
\code{\link{setup.model}()}
}
\concept{Utilities}
